# public/photos/

Drop photos of Giulia here. The carousel reads from `src/photos.ts` — add one entry per photo:

```ts
{ src: '/photos/your-file.jpeg', alt: 'Describe the moment', caption: 'Optional romantic line' }
```

Current archive: `1.jpeg`, `2.jpeg`, `3.jpeg` (mapped in `src/photos.ts`).